name: KBO-Bot
on:
  schedule:
    - cron: '*/10 * * * *' # 경기 중 점수 반영을 위해 10분마다 실행
  workflow_dispatch: 

concurrency:
  group: kbo-daily-ingest
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7
      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: pip install -r requirements.txt
      - name: Run tests
        run: python -m unittest discover -s tests
      - name: Run API tests
        run: node --test tests/*.test.mjs
      - name: Run Crawler
        env:
          KBO_INGEST_URL: ${{ secrets.KBO_INGEST_URL }}
          KBO_INGEST_TOKEN: ${{ secrets.KBO_INGEST_TOKEN }}
          HOLIDAY_API_KEY: ${{ secrets.HOLIDAY_API_KEY }}
          # 오늘 경기의 이닝·선수 상태를 실시간에 가깝게 보강한다.
          KBO_COLLECT_DETAILS: '1'
          KBO_DETAIL_WINDOW_DAYS: '0'
          # 네이버 game-polling으로 오늘 경기 점수·이닝·상태를 보강한다.
          KBO_NAVER_LIVE: '1'
        run: python crawling.py
